services:
  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres

  app:
    build: .
    ports:
      - 80:8080
    volumes:
      - .:/usr/app
    depends_on:
      - db
